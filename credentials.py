import os

# current stage of development is for school project
# credentials will not be necessery for project features
# credentials will be necessary for later stages of application

USERNAME = os.getenv("USERNAME")
PASSWORD = os.getenv("PASSWORD")